#include <stdio.h>

int main()
{
    long num1, num2, num3;
    scanf("%ld %ld %ld", &num1, &num2, &num3);
    //printf("%d %d\n", num1, num2);
    printf("%ld\n", num1 + num2 +num3);

    return 0;
}